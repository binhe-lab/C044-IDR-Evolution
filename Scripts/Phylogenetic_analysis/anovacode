rm(list=ls())
getwd()
#library(ape)
#library(geiger)
#library(phytools)

file_name=readline(prompt="Ener file name: ")
mol_feats_data<-read.csv(file_name, header=TRUE, stringsAsFactors=TRUE)
mol_feats_data
mol_feats_df<-data.frame(mol_feats_data[1:21,], stringsAsFactors = True)
mol_feats_df

mol_feats_model<-lm(REP_Q2 ~ Group, data=mol_feats_df)
anova_results<-anova(mol_feats_model)
anova_results

#variables<-colnames(mol_feats_data)
#variables

for(i in 3:ncol(mol_feats_df)){
  print(as.factor(colnames(mol_feats_df)[i]))
  #variable_fact<-as.factor(colnames(mol_feats_df)[i])
  #variable_fact
  mol_feats_model<-lm(as.factor(colnames(mol_feats_df)[i]) ~ Group, data=mol_feats_df)
  #mol_feats_model
  #anova_results<-anova(mol_feats_model)
  }
#anova_results
